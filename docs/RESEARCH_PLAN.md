# RESEARCH_PLAN.md

面向人（我自己）的文档，记录研究判断与决策依据。Claude Code 读它是为了理解上下文，不是为了执行。

---

## 核心判断

现有 token 压缩方法的局限可以归到同一句话：

> **它们优化的是代理指标（attention / saliency / diversity / coverage），而不是"答对这道题所需的证据"。**

把各家论文的"主要局限"列连起来看，这个模式非常清晰：注意力排序漏细节、多样性≠答题必需、显著区域未必是证据、覆盖度不保证数字和表格单元正确。

另一半线索来自 CVPR 2026 那篇"信息视界"分析：**OCR 所需的视觉信息持续到比普通 VQA 更深的层**。也就是说，"该在哪一层剪"本身是随 query 类型变化的。

合起来的候选问题陈述（**尚未验证，Phase 1 的任务就是验证或证伪它**）：

> 剪枝的**深度**（在哪层剪）和**依据**（用什么信号剪）不应是固定超参数，而应由该 query 的信息需求决定。

这个陈述如果成立，好处是它**不是** A+B：FastV 是"固定浅层"的特例，DyVTE 是"固定退出条件"的特例，pre-LLM 方法是"第 0 层"的特例。它们变成同一框架下的点，而不是被拼在一起的零件。

---

## 为什么先做基础设施而不是先做方法

三个理由：

1. **现有对比不可比。** 见 `PROTOCOL.md` §2.1 的 FastV 例子——同样叫 "64 tokens"，实际计算量差 50%。在错误的基准上设计方法，等于在噪声上做梯度下降。
2. **Oracle 上界能在一个月内判死刑。** 如果样本级最优融合只比最强单一方法高 0.5 分，那融合方向不存在，早知道早转向。
3. **诊断本身可发表。** 即使方法不成功，"TLB 对齐后排名变化 + random baseline 的真实位置 + ER 解释掉分"这套分析是独立成立的。近两年这类"诊断 + 由诊断导出的简单方法"的论文成功率明显高于增量式方法论文。

---

## 决策点

```
T1.1 无压缩 baseline 对不上官方？
   └─ 是 → 停，修实验台。别的都别做。

T1.2 random baseline 逼近各方法论文数字？
   ├─ 是 → 重大发现。方向转为"现有评测基准失效"的诊断论文。
   └─ 否 → 继续。

T1.6 TLB 对齐后排名与原论文不一致？
   ├─ 是 → 这是论文的第一张图。继续。
   └─ 否 → 继续，但需要更强的后续发现。

T1.7 Oracle gap
   ├─ < 1.0 点 → 融合/路由路线否决。转向纯诊断 + ER 指标论文。
   ├─ 1.0–3.0  → 继续，但 router 开销必须极小（< 5% 额外时延）。
   └─ > 3.0 点 → 继续，进入方法设计。

T1.8 换到 Qwen2.5-VL 后结论失效？
   └─ 是 → 这可能才是真正的论文："结论的模型依赖性"。
```

---

## Phase 2 候选方向（Phase 1 结束前不展开）

按当前优先级：

1. **Query-conditioned pruning depth** — 用极廉价探针（如仅基于 question embedding 的小分类器）预测该 query 的"信息视界"，据此决定剪枝层。核心卖点是统一解释而非叠加模块。
2. **Evidence-aware 评测框架** — 把 ER 做成这个领域缺失的诊断指标，配合"coverage 高但 ER 低 → 掉分"的因果论证。工作量可控，可独立成文。
3. **无监督 router** — 只有 T1.7 的 oracle gap > 3 点才考虑。

---

## 自我约束（写给我自己）

- 方法里的模块数 ≤ 2，新增超参 ≤ 3。超过就说明我在堆积而不是在解释。
- 任何 ablation 里贡献 < 0.5 点的模块，删掉。
- 绝不在"某个 benchmark 涨了 0.8 分"的基础上写论文。要么有机制解释，要么有明显的 gap。
- 遇到反常结果时，**先自己想，再问 AI**。AI 会给一个听起来合理的解释，然后我就错过了那个反常。
